version: '3.7'
services:
  db:
    container_name: sqlserver2017_demo
    image: alexk002/sqlserver2017_demo:1
    build:
      context: ./Docker/
      args:
        - SA_PASSWORD=$SA_PASSWORD
    environment:
      - SA_PASSWORD=${SA_PASSWORD}
      - SA_Pwd=${SA_PASSWORD}
      - ACCEPT_EULA=${ACCEPT_EULA}
      - MSSQL_AGENT_ENABLED=${MSSQL_AGENT_ENABLED}
      - MSSQL_PID=${MSSQL_PID}
    ports:
      - '1433:1433'
